services:

  # mysql:
  #   image: mysql:8.0
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-root123}
  #     MYSQL_DATABASE: ${DB_NAME:-stock_db}
  #   ports:
  #     - "3307:3306"
  #   volumes:
  #     - ./stock_data_MySQL:/var/lib/mysql

  python:
    build: .
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8501:8501"
      - "8888:8888"
      - "5000:5000"
    environment:
      - PORT=5000 
    env_file:
      - .env
